import React from 'react';
import Hero from '../Hero/Hero';

const Home = () => {
    return (
        <section className='container'>
            <Hero></Hero>
        </section>
    );
};

export default Home;