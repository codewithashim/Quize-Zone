import React from 'react';

const QuestionCart = ({currectAns}) => {
    console.log(currectAns);
    return (
        <section>
            <h1>Ans is {currectAns}</h1>
        </section>
    );
};

export default QuestionCart;